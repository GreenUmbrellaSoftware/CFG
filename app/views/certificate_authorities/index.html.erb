<p id="notice"><%= notice %></p>

    <div class="container">
      <div class="jumbotron">
        <h1>Certificate Authorities</h1>
        <p class="lead">In cryptography, a certificate authority or certification authority (CA) is an entity that issues digital certificates.  The Configurator allows its users to upload one or more CAs to be used for client authentication.</p>
        <p><%= link_to 'Add a Certificate Authority',
                   new_certificate_authority_path,
                   class: 'btn btn-lg btn-success' %>
      </div>

      <% unless @certificate_authorities.empty? %>
      <table class="table">
        <thead>
        <tr>
          <th>Title</th>
          <th>Intermediate</th>
          <th>Root</th>
          <th colspan="3"></th>
        </tr>
        </thead>

        <tbody>
        <% @certificate_authorities.each do |certificate_authority| %>
            <tr>
              <td><%= certificate_authority.title %></td>
              <td><%= display_common_name certificate_authority.certs[:intermediate] %></td>
              <td><%= display_common_name certificate_authority.certs[:root] %></td>
              <td><%= link_to 'Show', certificate_authority %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <% end %>

    </div>
